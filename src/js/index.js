// import Widget from "../../widget/dist/bundle";
// import Widget from "./widget/dist/bundle";

// mount the Svelte component on the page
document.addEventListener("DOMContentLoaded", () => {
    new MyComponent({
        target: document.getElementById("widget"),
    });
});
